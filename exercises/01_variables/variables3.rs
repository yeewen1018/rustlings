fn main() {
    // TODO: Change the line below to fix the compiler error.
    //let x: i32;

    //println!("Number {x}");

    // Again, the above code is not able to compile 
    // because Rust does not allow uninitialized variables; 
    let x = 32; 
    println!("Number {x}"); 
}
